---
modules:
  - type: dnf
    install:
      install-weak-deps: false
      packages:
        - pipewire-jack-audio-connection-kit

  - type: dnf
    repos:
      cleanup: true
      copr:
        - ycollet/audinux
    install:
      install-weak-deps: false
      packages:
        - cardinal
...

---
modules:
  - type: dnf
    install:
      install-weak-deps: false
      packages:
        - foot
        - xdg-terminal-exec

  - type: systemd
    user:
      enabled:
        - foot-server.socket

  - type: files
    files:
      - source: wm/common/foot
        destination: /
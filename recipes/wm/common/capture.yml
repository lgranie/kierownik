---
modules:
  - type: dnf
    repos:
      cleanup: true
      copr:
        - brycensranch/gpu-screen-recorder-git
    install:
      install-weak-deps: false
      packages:
        - grim
        - slurp
        - gpu-screen-recorder

  - type: files
    files:
      - source: wm/common/capture
        destination: /
...

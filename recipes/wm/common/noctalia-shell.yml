---
modules:
  - type: dnf
    repos:
      cleanup: true
      copr:
        - zhangyi6324/noctalia-shell
    install:
      install-weak-deps: false
      packages:
        - noctalia-qs
        - noctalia-shell

  - type: files
    files:
      - source: wm/common/noctalia-shell
        destination: /
---
modules:
  - type: dnf
    repos:
      cleanup: true
      copr:
        - lionheartp/Hyprland
    install:
      install-weak-deps: false
      packages:
        - noctalia-qs
        - noctalia-shell
        - power-profiles-daemon

  - type: files
    files:
      - source: wm/common/noctalia-shell
        destination: /
...

---
modules:
  - type: dnf
    install:
      install-weak-deps: false
      packages:
        - niri

  - type: files
    files:
      - source: wm/niri
        destination: /
  
  - type: script
    snippets:
      - "chmod +x /usr/bin/start-niri-session"

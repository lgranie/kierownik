# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
---
modules:
  - type: script
    snippets:
      - "chmod -R +x /usr/lib/kierownik/tasks"

  - from-file: finalize/container.yml
  - from-file: finalize/bootc.yml
...

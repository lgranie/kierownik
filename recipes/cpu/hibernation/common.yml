---
# Hibernation configuration
modules:
  - type: kargs
    arch: x86_64
    kargs:
      - mem_sleep_default=deep

  - type: files
    files:
      - source: cpu/hibernation
        destination: /

  - type: systemd
    system:
      enabled:
        - hibernation-configure.service
        - hibernation-check.service

---
modules:
  - type: dnf
    install:
      install-weak-deps: false
      packages:
        - plymouth-theme-spinner
      exclude:
        - fedora-logos

  - type: script
    snippets:
      - "plymouth-set-default-theme spinner"

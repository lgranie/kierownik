---
modules:
  - type: dnf
    install:
      install-weak-deps: false
      packages:
        - usbutils
        - kernel-tools

  - type: files
    files:
      - source: base/hibernation
        destination: /

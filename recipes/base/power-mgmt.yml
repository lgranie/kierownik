---
modules:
  - type: dnf
    install:
      install-weak-deps: false
      packages:
        - kernel-tools
        - power-profiles-daemon

  - type: systemd
    system:
      enabled:
        - power-profiles-daemon.service
...

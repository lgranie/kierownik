---
modules:
  - type: systemd
    system:
      disabled:
        - dnf-makecache.service
        - dnf-makecache.timer
        - raid-check.service
        - raid-check.timer
        - rpm-ostree-countme.service
        - rpm-ostree-countme.timer
        - sshd.service
        - sshd.socket
    user:
      enabled:
        - podman.socket

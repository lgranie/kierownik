---
modules:
  - type: dnf
    install:
      install-weak-deps: false
      packages:
        - fish

  - type: files
    files:
      - source: base/fish
        destination: /

  - type: script
    snippets:
      - "sed -i 's|^SHELL=.*|SHELL=/usr/bin/fish|' /etc/default/useradd"

---
modules:
  - type: files
    files:
      - source: wsl/system
        destination: /

  - type: script
    snippets:
      - "chmod +x /usr/bin/oobe /usr/bin/hypr-wsl-nested"

  - from-file: wsl/systemd.yml

  - from-file: wsl/base.yml
...

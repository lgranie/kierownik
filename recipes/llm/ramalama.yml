---
modules:
  - type: dnf
    install:
      install-weak-deps: false
      packages:
        - nvtop
        - ramalama

  - type: files
    files:
      - source: llm/ramalama
        destination: /

  - type: script
    snippets:
      - "firewall-offline-cmd --zone=Kierownik --add-service=ramalama"
...

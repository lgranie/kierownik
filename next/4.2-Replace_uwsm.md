# Replace uwsm with systemd units + systemd-run

## Goal

- Drop `uwsm` session manager. Single Hyprland setup.
- Replace with own systemd units (`hyprland.service`, `hyprland-session.target`) + `systemd-run --user --scope` for app launch.
- No compositor param: units and `start-hyprland-session` are Hyprland-specific (generic `wayland-wm@` template removed).
- Finish local `systemd-run` branch (already rebased on `main`), fix leftovers, validate, publish.

## Status (2026-09-17, verified)

- Local `systemd-run` = `main 843f07b` + 2 commits: `6c8226a` (revert uwsm merge), `9b4bc14` (systemd-run for apps).
- `origin/systemd-run` stale (~191 behind). Local ahead 191 / behind 2 vs origin due to rewrite. Publish needs force-push.
- Diff `main..systemd-run` now small: 13 files, ~96 insertions, ~24 deletions. Old 254-file diff was against stale origin ref.
- Branch already uses `--scope`: `files/wm/common/hypr/usr/share/hypr/bindings.lua` has `systemd-run --user --scope -- footclient` (all three binds). Good.
- fsel config no longer exists on branch (`files/base/tools/etc/fsel/config.toml` gone). No `launch_prefix` fix needed.

## What uwsm did vs branch coverage

- `uwsm start`: env setup, import into user manager, bind `graphical-session.target`, scope tracking, cleanup on exit. Covered by `hyprland.service` (Type=notify, Slice=session.slice, Before/PropagatesStopTo `graphical-session.target`, ExecStart=`/usr/bin/Hyprland`), `hyprland-session.target` (BindsTo `graphical-session.target`), shutdown target, `start-hyprland-session` (tty1 guard, already-running guard, `import-environment`, `start --wait`).
- `uwsm app`: scope launch with env. Covered by `systemd-run --user --scope --`.
- `uwsm stop`: teardown. Covered by OnSuccess/OnFailure -> `hyprland-session-shutdown.target`.
- User services (`sunshine`, `voxtype`, `wvkbd`) bind `graphical-session.target`. Keep working.
- Greetd switched: `hyprland-uwsm` -> `hyprland` (`files/wm/common/greetd/etc/greetd/config.toml`). Good.
- Autostart keeps `dbus-update-activation-environment --all`. Good.

## Leftovers (must fix before publish)

1. `files/wm/common/hypr/usr/share/hypr/autostart.lua:16` still calls `os.execute("uwsm stop && sleep 0.1")` on `hyprland.shutdown`. Package gone. Shutdown handled by units. Remove call (keep comment about killing troublesome services if still wanted).
2. `files/wm/common/noctalia/usr/lib/systemd/user/noctalia.service:6` still has `After=graphical-session.target wayland-session@hyprland.desktop.target`, plus `WantedBy=wayland-session@hyprland.desktop.target`. Second target is uwsm-specific, dangles without uwsm. Fix: keep only `graphical-session.target` in both directives.
3. `files/wm/common/hypr/usr/share/wayland-sessions/hyprland.desktop:6` missing newline at EOF. Add newline.
4. `recipes/wm/common/hypr.yml:23` still runs `rm -f /usr/share/wayland-sessions/hyprland-uwsm.desktop`. Harmless after source deleted. Drop after one build cycle.
5. `recipes/wm/common/uwsm.yml` deletion also drops `pciutils`. Check whether `pciutils` needed elsewhere (e.g. scripts, tasks). Re-add to another recipe if needed.
6. `next/0.0-clean_system_user_services.md`, `next/1.0-autologin-fido.md`, `next/1.1-autologin-gpg.md` still reference `hyprland-uwsm` / `uwsm start`. Docs only. Update separately if those plans still live.
7. Untracked `next/4.1-Disable_XWayland.md` present. Decide: keep, move, delete. Unrelated to this work.
8. `stash@{0}` touches plymouth, kargs, recipe renames. Unrelated. Decide: apply, keep stashed, or drop.

## Changes

1. `files/wm/common/hypr/usr/share/hypr/autostart.lua`: remove `uwsm stop` line from shutdown handler.
2. `files/wm/common/noctalia/usr/lib/systemd/user/noctalia.service`: reduce After/WantedBy to `graphical-session.target` only.
3. `files/wm/common/hypr/usr/share/wayland-sessions/hyprland.desktop`: add trailing newline.
4. Optional: `recipes/wm/common/hypr.yml`: drop `rm -f ...hyprland-uwsm.desktop` snippet once image confirmed clean.
5. Optional: restore `pciutils` in appropriate recipe if anything depends on it.
6. No changes needed: greetd config, bindings `--scope`, hyprland units (`files/wm/common/hypr/usr/{bin/start-hyprland-session,lib/systemd/user/hyprland{,-session,-session-shutdown}.{service,target}}`), `hyprland.desktop` Exec.

## Verification

- `mise run check:lint`.
- `mise run build:oci 5290` (plus 8745 if Hyprland recipe shared).
- `mise run run:vm 5290`: login via greeter, check `systemctl --user status graphical-session.target hyprland-session.target hyprland.service`, env (`WAYLAND_DISPLAY`, `XDG_CURRENT_DESKTOP`), app scope (`systemd-cgls --user`), SUPER+RETURN launches footclient in scope, clean exit on compositor quit, second `start-hyprland-session` run blocked by already-running guard.
- Confirm no `uwsm` strings remain outside `next/*.md` docs and the intentional `rm -f` cleanup: `git grep -n uwsm -- files recipes`.

## Publish

- Requires `git push --force-with-lease origin systemd-run` (history rewritten by rebase). Irreversible for other trackers of that remote branch. Confirm approval first.
- Decide stash + untracked file disposition before push.
- Open PR if review needed.

## Decisions needed

- Force-push approval for `origin/systemd-run`?
- Stash apply / drop? Untracked `next/4.1-Disable_XWayland.md` keep?
- `pciutils`: restore elsewhere or drop with uwsm?
- Update `next/0.0, 1.0, 1.1` docs to `hyprland` session name now, or leave?

[Service]
# Hyprland prints ~50 DEBUG lines to stdout before it parses
# debug.enable_stdout_logs=false, all at PRIORITY=6. Drop info/debug
# from the journal; warnings+ still land, full log stays in $XDG_RUNTIME_DIR/hypr/*/hyprland.log.
LogLevelMax=warning

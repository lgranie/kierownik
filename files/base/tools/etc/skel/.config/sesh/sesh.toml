#:schema https://github.com/joshmedeski/sesh/raw/main/sesh.schema.json

# cache = true

[default_session]
preview_command = "lsd --tree --depth 2 {}"

[[window]]
name = "agent"
startup_script = "opencode"

[[window]]
name = "editor"
startup_script = "nvim -c 'lua Snacks.picker.files()'"

[[window]]
name = "git"
startup_script = "lazygit"
